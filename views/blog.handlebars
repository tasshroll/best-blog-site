<div class="text-center">
  <h2>{{title}}</h2>
  <p>{{post}}</p>
  <h4>Created by {{user.name}} on {{format_date date_created}}</h4>
  <h3>Prior Coments</h3>



  {{#if comments.length }}
    {{#each comments}}
      <div>
        <p>{{this.content}}</p>
      </div>
    {{/each}}
  {{/if}}


  {{!-- <p>
    <span for="img" aria-label="money">💲</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p> --}}
  <form class="form comment-form">
    <div class="form-group">
      <label for="project-desc">Post Your Comment Below:</label>
      <textarea class="form-input" id="comment-content" name="comment-content"></textarea>
    </div>
    <div class="form-group">
      <button type="submit" class="btn btn-primary">Post Comment</button>
    </div>
  </form>
  {{!-- <p>
    <button class="btn btn-primary">Fund this Project!</button>
  </p> --}}
</div>

<script src="/js/comment.js"></script>